server:
  port: 9296
  
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/tonilxm/dailycodebuffer
          clone-on-start: true
